/** @type {import('next').NextConfig} */
const nextConfig = {
  /** render component twice in dev env, set to false to disable it */
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
